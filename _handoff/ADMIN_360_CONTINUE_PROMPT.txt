You are continuing implementation work on NobleClean-Ops. Before writing
any code, read _handoff/PROGRESS.md end to end, in particular the final
section "Admin 360 Drilldown and Media — GAP 1: Employee Detail Page and
Real Weekly Availability" and its "Pre-existing regression found during
this session" subsection. Then read _handoff/NobleClean-Ops_PRD.md,
_handoff/DESIGN.md, _handoff/SECURITY_RULES.md, and
_handoff/VALIDATION_DTO_CONVENTIONS.md end to end. This project uses bd
(beads) for ALL task tracking per AGENTS.md and CLAUDE.md; do not use
TodoWrite, TaskCreate, or markdown TODO lists. Start with `bd prime` and
`bd ready`.

STATUS CONFIRMED BY DIRECT REPO AUDIT: GAP 1 (employee detail page and
real employee_weekly_availability table) is fully implemented and closed
per PROGRESS.md. GAP 2 through GAP 6 below are NOT implemented yet. Do
not redo GAP 1. Do not re-introduce getDeterministicAvailability logic
anywhere.

STEP 0 (do this before any new feature work, and treat it as its own
Beads issue): Fix the pre-existing regression documented in PROGRESS.md:
`npm run lint` currently fails with roughly 59 errors (mostly unused
imports) across files unrelated to GAP 1, `npm run security:patterns`
fails on src/components/ui/view-toggle.tsx (SEC-STORAGE-001, browser-
readable storage of auth/session-adjacent state), and `npm run test`
fails 8 of 60 tests. Fix all three fully: remove unused imports flagged
by lint, remove or properly scope the flagged storage usage in
view-toggle.tsx so it no longer stores auth/session-adjacent state in
browser-readable storage, and fix the 8 failing tests (do not just
change the assertions to match broken behavior — find and fix the
actual regression). Confirm `npm run quality:full` passes clean before
moving on. Record exactly what caused the regression in PROGRESS.md so
it is not silently reintroduced again.

Then file a Beads epic named "Admin 360 Drilldown and Media" (reuse the
existing one if `bd ready`/`bd prime` shows it already exists from a
prior session) with one child issue per gap below, ordered by
dependency, and implement each child issue fully before moving to the
next, exactly the way GAP 1 and every other closed issue in
_handoff/PROGRESS.md was implemented: strict Zod schemas (.strict()),
normalized command DTOs, explicit Supabase mutation objects (never
.update(dto)/.upsert(dto) directly), allowlisted response DTOs,
server-side admin authorization via requireRole, same-origin checks
(hasSameOriginRequest) on every mutation, RLS on every new table,
pgTAP coverage under supabase/tests, unit tests under tests/unit,
German and English message keys with no hardcoded UI copy and no
Arabic UI text, and no service-role/secret-key usage anywhere in
application code.

GAP 2: Default daily working hours per employee.
Add a constrained numeric default_daily_hours value for employee
profiles (greater than 0, less than or equal to 24), editable from the
existing employee detail page at src/app/[locale]/admin/staff/[employeeId].
When the admin creates a new work_schedule row for that employee,
pre-fill allocated_hours from this default while still allowing a
per-date override. Follow the same migration/DTO/action pattern used
for GAP 1's employee_weekly_availability work.

GAP 3: Employee and client photos.
Add nullable avatar_path columns to profiles and clients following the
exact private-storage pattern already used for
sections.reference_image_path and leaf_items.reference_image_path in
supabase/migrations/20260729002000_reference_image_storage.sql: private
bucket, strict jpeg/png/webp MIME allowlist, file size limit, RLS-scoped
random object naming with no user-provided filenames, and server-side
magic-byte validation. Design an equivalent safe object-naming scheme
scoped to profile/client avatars and document it in _handoff the same
way the original storage migration is documented. Every place a client
or employee name renders anywhere in the admin UI must show their
avatar with an initials-based fallback when unset. Add upload/change
controls to the client form and to the employee detail page.

GAP 4: Client detail page.
There is no /admin/clients/[clientId] route today, only a flat list at
src/app/[locale]/admin/clients/page.tsx. Add one, following the exact
same dynamic-route/guard/DTO pattern GAP 1 established for
/admin/staff/[employeeId]. Every client name anywhere in the admin UI
must link to it. This page must show in one place: client profile
fields and photo, the full section/sub-section tree for that client
with images at every depth, assigned employees current and historical,
that client's recent and upcoming schedule, recent daily plans and
completion stats, and any overdue mandatory tool-step escalations,
reusing the existing src/features/admin/reports read layer instead of
duplicating query logic. Every employee, section, and leaf item name
shown here must itself link to its own detail view per GAP 6.

GAP 5: Properly designed unlimited nested sub-sections.
The section self-reference and cycle-prevention constraints in
supabase/migrations/20260728233000_initial_schema.sql already support
unlimited depth; do not weaken them. Audit the section tree UI in
src/app/[locale]/admin/sections-items. If it renders as flat, shallow,
cramped, or visually stacked, redesign it using existing
src/components/ui primitives and Operational Clarity tokens so
arbitrarily deep nesting is visually clear: consistent per-depth
indentation, a breadcrumb trail when drilled into a subsection, section
thumbnail images at every depth (using GAP 3's avatar/image pattern if
applicable), and generous spacing on both desktop and mobile widths.

GAP 6: Global click-through navigation and edit-from-anywhere.
Add one shared client component used everywhere a client, employee,
section, or leaf item name/avatar renders across the admin surface,
that always navigates to that entity's own detail location (GAP 1's
employee route, GAP 4's client route) regardless of which admin page
the click happened on. Where inline mutation without leaving the
current screen is appropriate and already consistent with the PRD and
SECURITY_RULES.md (for example toggling an assignment, editing a leaf
item tag, or adjusting allocated_hours from the client detail page),
implement it as a Server Action following the same conventions as
existing src/features/admin/*/actions.ts files. Do not weaken any RLS
policy or role guard; all navigation still passes through the existing
admin layout role guard.

After each child issue: run `supabase db reset --local` if migrations
changed, then `npm run quality:full`, and report exact pass/fail counts
the same way every existing PROGRESS.md entry does. Update
_handoff/PROGRESS.md with one new dated section per completed child
issue in the same style as existing entries; do not create a separate
tasks.md file. Close each Beads issue only after its local quality
gates pass.

IMPORTANT ABOUT GIT: run `git remote -v` yourself before assuming
anything about push state. Do not assume the state described in older
PROGRESS.md entries is still accurate; verify it fresh in this session,
and document what you actually found.

When every child issue (including the Step 0 regression fix) is closed,
run `bd stats` and `bd ready`, print a summary of everything changed,
then follow the Session Completion workflow defined in CLAUDE.md: file
follow-up issues for anything left, run quality gates, close finished
issues, and attempt `git pull --rebase` then `git push` against whatever
remote `git remote -v` actually shows in this session. If no git remote
is configured, stop before the push step and say so clearly instead of
failing silently. Work fully autonomously end to end without waiting
for further human input; only stop early if a genuine external blocker
(missing credentials, missing remote, ambiguous product decision not
answerable from the handoff docs) makes further progress impossible, and
document that blocker clearly in PROGRESS.md exactly like it has been
documented for previous blockers.
